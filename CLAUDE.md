# CLAUDE.md

这个仓库是一份英语学习笔记,记录在真实阅读(技术文档、书籍、工作沟通)中遇到的单词、短语和语法点。

## 对话方式

- **始终用中文回复解释部分**,无论用户用中文还是英文提问。
- 用户贴一段文字并问其中某个词/短语时,**先解释它在那段语境里的含义**,再扩展到其他义项,不要一上来就给通用词典释义。
- 单词条目要给 **CEFR 等级**和**音标**(英式/美式有明显差异时都给)。
- 一词多义时,**按义项分组**给例句,不要把不同含义的例句混在一起。
- 有值得一提的固定搭配时(如 canary test / canary release / canary deployment),单独列出来并说明它们共通的语感。
- **用户的目标是把写作提升到 C1 水平**。检查用户写的句子时,除了指出错误,**还要给出 C1/母语者会怎么表达同样的意思**——即使原句语法没错但不够地道,也要给出更自然的版本。

## 什么时候写入 vocabulary.md

**只有用户明确说"存""save""go""记一下"之类的确认之后才写入。** 讲解完可以主动问一句"要存进 vocabulary.md 吗?",但不要不问自存。

写入之后**自动提交并推送到 `dev` 分支**,不用再问一次。提交信息写清楚加了哪个词条,例如 `Add entry: deprioritize`;一次确认多个词条时合成一个提交。

**绝对不要直接推送到 `main`,也不要自行发起或合并 PR。** `dev` 上的推送不会触发构建;只有 `main` 会发布页面,而**什么时候把 dev 合进 main 由用户自己决定**(他会去开 PR)。如果用户明确要求,才做 main 相关的操作。

### 写入前必须脱敏

**这个仓库是公开的。** 用户拿来检查的句子经常直接来自工作场景(发布通告、工单、Slack 消息),这类句子**不能原样写进 vocabulary.md**。写入前把下面这些换掉,语法点和用词要点原样保留:

- **公司名、产品名、项目代号** → 通用说法。例:`Lofty Android has released 4.50.0` → `Our Android app has released a new version`;`DocuSign` → `an e-signature service`。
- **人名** → `a teammate` / `a colleague` / `the reviewer`。
- **内部链接、工单号、账号、邮箱、服务器名、内部环境名** → 直接删掉或换成通用描述。
- **具体到能定位内部情况的细节**(某次事故、某个客户、某个未公开的功能)→ 改写成通用场景。

如果一句话脱敏之后就失去了原本要演示的语言点,**就另造一个能说明同样问题的中性例句**,不要为了保留原句而把内部信息带进去。用户原句里的语言问题照常在对话里讲解——**脱敏只针对写进仓库的内容**。

## 条目格式

```markdown
---

<a id="锚点"></a>
## 单词或短语

- 音标:英 /.../ 美 /.../   (短语或英美一致时可省略)
- 词性:n. / v. / phrase / ...  (短语不适用时可省略)
- CEFR:B2                      (无标准分级的短语可省略)

**English definition:** 一句英文释义(单词条目必给)

**含义:** 中文解释

**义项一:<义项说明>**       (多义词才分义项)
1. English example sentence.
   中文翻译。
2. Another example sentence.
   中文翻译。

**常见搭配:** ...            (有值得记的固定搭配时才写)
```

### 整句订正条目(句子:)

用户拿自己写的句子来检查、订正之后要存的,用这个格式。标题以 **`句子:`** 开头,后面跟这句话涉及的**核心搭配**(便于搜索和回忆),构建脚本靠这个前缀把它归到页面上的"句子"分区。

```markdown
---

<a id="s-锚点"></a>
## 句子:核心搭配A / 核心搭配B

**我的原句:**

> 用户写的原句(脱敏后)

**问题:**

- ❌ **错误点** —— 为什么错、正确的说法是什么
- ⚠️ **不地道之处** —— 能懂但母语者不会这么说,原因

**更地道的说法:**

1. C1/母语者版本。
   中文翻译。(可附一句说明为什么这样更好)
2. 另一个版本。
   中文翻译。

**涉及的搭配:**

- **collocation** —— 中文解释。*Example sentence.*(中文翻译。)
```

短语和搭配**不必单独开条目**,直接并进它出现的那个句子条目的"涉及的搭配"里——这样搭配能带着真实语境一起记。只有当一个词本身值得独立展开(多义项、CEFR、词族)时,才另开单词条目。

卡片在页面上会用**原句**做预览摘要,所以 `> ` 引用块里放的必须是用户的原句,不是订正后的版本。

### 单词/短语条目的写入规则

- **每个义项最多 3 个例句,2 个为宜**,第 3 句只在确实能补充覆盖面时才加。
- **每个英文例句下面紧跟中文翻译**,不要只给英文。
- 单词条目**必须有英文释义**(`English definition`),放在中文解释前面——这是在中文解释之外额外要给的,不是替代。
- **不要用 markdown 表格。** 多维度对比(词 × 语域 × 地道程度)一律用嵌套列表,加粗做小标题。
- **追加,不要改动已有条目**,除非用户要求订正。
- 追加新条目时,**同时在文件顶部 `## 目录` 里加一行** `N. [标题](#锚点)`,序号接上一条,锚点要和正文里的 `<a id="">` 对应。中文标题和标点会破坏 markdown 自动生成的锚点,所以必须手写 `<a id="">`。

## 分支与发布流程

- **`dev`** —— 日常分支,新词条都提交到这里,推送不触发任何构建。
- **`main`** —— 发布分支,一旦有内容变更就会触发 Action 构建并部署到 Pages。
- 用户开一个 `dev → main` 的 PR 并合并,页面才更新。

`index.html` 是构建产物,**不纳入版本管理**(见 `.gitignore`),由 Action 直接部署到 Pages。这样 dev 和 main 不会因为生成文件而分叉,合并时也不会在 `index.html` 上冲突。

## 重新生成网页

`vocabulary.md` 是唯一的内容来源,`index.html` 由脚本生成,**不要手工编辑 index.html**——要改页面就改 `template.html`。

```bash
python3 build.py                        # 本地预览用,生成 index.html(已被 gitignore)
python3 build.py --artifact out.html    # 额外输出用于发布 Claude artifact 的去壳版本
```

本机不需要跑 build.py 也能提交——构建由 Action 在 main 上完成。只有想在本地看效果时才跑。
