# CLAUDE.md

这个仓库是一份英语学习笔记,记录在真实阅读(技术文档、书籍、工作沟通)中遇到的单词、短语和语法点。

## 对话方式

- **始终用中文回复解释部分**,无论用户用中文还是英文提问。
- 用户贴一段文字并问其中某个词/短语时,**先解释它在那段语境里的含义**,再扩展到其他义项,不要一上来就给通用词典释义。
- 单词条目要给 **CEFR 等级**和**音标**(英式/美式有明显差异时都给)。
- 一词多义时,**按义项分组**给例句,不要把不同含义的例句混在一起。
- 有值得一提的固定搭配时(如 canary test / canary release / canary deployment),单独列出来并说明它们共通的语感。
- **用户的目标是把写作提升到 C1 水平**。检查用户写的句子时,除了指出错误,**还要给出 C1/母语者会怎么表达同样的意思**——即使原句语法没错但不够地道,也要给出更自然的版本。

## 什么时候写入 vocabulary.md

**只有用户明确说"存""save""go""记一下"之类的确认之后才写入。** 讲解完可以主动问一句"要存进 vocabulary.md 吗?",但不要不问自存。

## 条目格式

```markdown
---

<a id="锚点"></a>
## 单词或短语

- 音标:英 /.../ 美 /.../   (短语或英美一致时可省略)
- 词性:n. / v. / phrase / ...  (短语不适用时可省略)
- CEFR:B2                      (无标准分级的短语可省略)

**English definition:** 一句英文释义(单词条目必给)

**含义:** 中文解释

**义项一:<义项说明>**       (多义词才分义项)
1. English example sentence.
   中文翻译。
2. Another example sentence.
   中文翻译。

**常见搭配:** ...            (有值得记的固定搭配时才写)
```

写入时的规则:

- **每个义项最多 3 个例句,2 个为宜**,第 3 句只在确实能补充覆盖面时才加。
- **每个英文例句下面紧跟中文翻译**,不要只给英文。
- 单词条目**必须有英文释义**(`English definition`),放在中文解释前面——这是在中文解释之外额外要给的,不是替代。
- **不要用 markdown 表格。** 多维度对比(词 × 语域 × 地道程度)一律用嵌套列表,加粗做小标题。
- **追加,不要改动已有条目**,除非用户要求订正。
- 追加新条目时,**同时在文件顶部 `## 目录` 里加一行** `N. [标题](#锚点)`,序号接上一条,锚点要和正文里的 `<a id="">` 对应。中文标题和标点会破坏 markdown 自动生成的锚点,所以必须手写 `<a id="">`。

## 重新生成网页

`vocabulary.md` 是唯一的内容来源,`index.html` 由脚本生成,**不要手工编辑 index.html**——要改页面就改 `template.html`。

```bash
python3 build.py                        # 生成 index.html
python3 build.py --artifact out.html    # 额外输出用于发布 Claude artifact 的去壳版本
```

推送到 `main` 后 GitHub Action 会自动重新生成并提交 `index.html`,页面 https://chinalwb.github.io/vocab/ 几分钟后更新。所以在本机改完 markdown 后,直接提交推送即可,不必先在本地跑 build.py(跑了也无妨)。
